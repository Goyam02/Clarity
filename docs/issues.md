when i try to run a calibration quiz while signing up, this error pops up
FOUNDRY_AUTH_FAILED: Foundry authentication failed: no Entra credential is available to the backend. Easiest fix: set AZURE_FOUNDRY_API_KEY in backend/.env (Foundry project API key), or give the container a service principal (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET). See docs/azure-setup.md.


even though this API key, i was not able to configure at the foundry since its not available

same error is showing up 

FOUNDRY_AUTH_FAILED: Foundry authentication failed: no Entra credential is available to the backend. Easiest fix: set AZURE_FOUNDRY_API_KEY in backend/.env (Foundry project API key), or give the container a service principal (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET). See docs/azure-setup.md.

when i am trying to do the skill confidence mapping


when i try to click on generate plan on the http://localhost:3000/dashboard?welcome=1 route, 
this error is showing up
FOUNDRY_AUTH_FAILED: Foundry authentication failed: no Entra credential is available to the backend. Easiest fix: set AZURE_FOUNDRY_API_KEY in backend/.env (Foundry project API key), or give the container a service principal (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET). See docs/azure-setup.md.


FOUNDRY_AUTH_FAILED: Foundry authentication failed: no Entra credential is available to the backend. Easiest fix: set AZURE_FOUNDRY_API_KEY in backend/.env (Foundry project API key), or give the container a service principal (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET). See docs/azure-setup.md.

same error on this apge as well
http://localhost:3000/code-red


Foundry authentication failed: no Entra credential is available to the backend. Easiest fix: set AZURE_FOUNDRY_API_KEY in backend/.env (Foundry project API key), or give the container a service principal (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET). See docs/azure-setup.md.

same error on http://localhost:3000/onboarding 


also clicking on start revision button in the today;s revision on http://localhost:3000/dashboard route should not load up the knowledge graph but something else, maybe a new route or something


also clicking on Launch Mock OA shows this error
Could not load the assessment. Is the backend running and signed in?
Back to Dashboard

