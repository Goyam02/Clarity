okay
the leetcode sync is implemented but i do not see it in the UI first
in the UI
there is still that upload profile ss page on boarding that should be removed
and on the next screen just after the upload ss, there should be option to add leetcode as well in addition to githb and codeforces
also the leetcode CSRF token and Leetcode session token should be added over there as well
in the UI



also, implement judge self hosting locally in this single repository only
so that every service is up using a single docker compose up
no need to set it up externally (update docs/plan/plan-judge0-judge.md accordingly)
and the livekit should be replaced with gemini voice to voice api in the frontend only for the intrerview thing 

implement all these
but before it
read the entire codebase to get the context
also the .env has foundry endpoint setup but when i upload my resume, it shows 500 error and azure endpoint not set up