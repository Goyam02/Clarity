fastapi==0.116.1
uvicorn[standard]>=0.35
sqlalchemy==2.0.41
alembic==1.13.3
pydantic==2.11.7
pydantic-settings==2.10.1
httpx==0.28.1
python-multipart>=0.0.20
pypdf>=5.0
pytest==8.3.4
pytest-asyncio==0.24.0
anyio>=4.9.0
openai>=3.0.0
azure-ai-projects>=2.0
azure-identity>=1.17
# Required by azure-core's AioHttpTransport (DefaultAzureCredential's default
# async pipeline); its absence broke the unconfigured-vision error-path test.
aiohttp>=3.9
# PostgreSQL driver (primary database) + cache
psycopg2-binary>=2.9
redis>=5.0
# Token encryption for user-supplied platform cookies (LeetCode pulls)
cryptography>=43.0
# Optional (only needed for vision extraction; PDF text parsing uses pypdf above):
# azure-search-documents>=11.6
